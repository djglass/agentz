[project]
name = "agentz"
version = "0.1.0"
description = "LLM-powered threat intel CLI"
requires-python = ">=3.10"
dependencies = [
    "typer",
    "requests"
]


[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
agentz = "agentz.cli:app"
